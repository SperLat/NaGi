// Web stub — expo-sqlite requires WASM not available in Metro web bundler.
// All web testing runs in mock mode; localDb is never called.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const localDb = null as any;
export function initLocalDb(): void {}
